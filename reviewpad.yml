api-version: reviewpad.com/v3.x

rules:
  - name: tautology
    kind: patch
    spec: 1 == 1

workflows:
  - name: rebase
    always-run: true
    if:
      - rule: tautology
    then:
      - $rebase()